apiVersion: v1
kind: Secret
metadata:
  name: vault-ssl-cert
  namespace: vault-deployment
type: kubernetes.io/tls
data:
  tls.crt: {{ .Files.Get "certs/vault.crt" | b64enc }}
  tls.key: {{ .Files.Get "certs/vault.key" | b64enc }}
